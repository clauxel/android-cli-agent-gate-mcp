# Use Cases

- Android CLI agent build gate: evaluate Android CLI agent build gate with public-safe inputs.
- Android CLI Agent Gate: evaluate Android CLI agent build gate with public-safe inputs.
- Android CLI Agent Gate documentation: evaluate Android CLI agent build gate with public-safe inputs.
- Android CLI Agent Gate remote MCP: evaluate Android CLI agent build gate with public-safe inputs.
- androidcliagentgate server card: evaluate Android CLI agent build gate with public-safe inputs.

## Good First Pilot

Pick one bounded team workflow, use non-sensitive examples, and verify the generated evidence with the person who owns the decision.
